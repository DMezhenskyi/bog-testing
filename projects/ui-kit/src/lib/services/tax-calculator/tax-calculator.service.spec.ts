describe('TaxCalculatorService', () => {

  it.todo(`should properly calculate VAT for a given country`);
  
  it.todo(`should return 0 if isB2B flag is true`);
  
  describe(`TaxCalculatorService: Error Handling`, () => {
    
    it.todo(`should throw error if country isn't supported`);
    
    it.todo(`should throw error if price is negative number`);
  
  });
});
